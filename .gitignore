# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Editors / OS
.DS_Store
.idea/
.vscode/

# Pipeline scratch
pipeline/story_raw.json
pipeline/text_to_story.report.json
pipeline/plan.json
pipeline/review.json
pipeline/review.md

# Pipeline backups — derived from data/{vocab,grammar}_state.json on every ship.
# Tracked-in-git backups silently bloat the repo: 77 files / 19MB accumulated
# before this rule was widened on 2026-05-01.
state_backups/
!state_backups/.gitkeep

*.log

# Credentials
.gcp-key.json
*.gcp-key.json
pipeline/*.bak

# Node / SvelteKit
node_modules/
.svelte-kit/
build/
.vite/
*.tsbuildinfo
.env
.env.*
!.env.example
playwright-report/
test-results/
coverage/
.lighthouseci/

# Derived data artifacts (regenerated from data/vocab_state.json + stories/ at build time)
data/vocab/
data/grammar_examples.json
stories/index/
stories/index.json

# In-skill author scratch dir (prosecution tables, working notes per SKILL §E.5)
.author-scratch/
