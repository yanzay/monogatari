# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Editors / OS
.DS_Store
.idea/
.vscode/

# Pipeline scratch — intermediate outputs of individual authoring stages.
# Shipped artifacts live under stories/ and audio/.
pipeline/story_raw.json
pipeline/text_to_story.report.json
pipeline/plan.json
pipeline/review.json
pipeline/review.md

# Pipeline backups (kept locally for safety, not committed)
state_backups/normalize_to_v2/
state_backups/regenerate_all_stories/

# Service worker / browser caches (none here, but reserve)
*.log

# Credentials — never commit these
.gcp-key.json
*.gcp-key.json
pipeline/*.bak
